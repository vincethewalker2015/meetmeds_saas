<div class="row">
  <div class="col-md-6 col-md-offset-3 text-center">
    <div class="well form-color-style name-font">
      <% if params[:plan] == '2' %>
      <h1>Premium Account</h1>
      <p>Create a premium account for £7/month and get full access to the community and see all contact information</p>
      <% else %>
        <h1>Free Account</h1>
        <p>Join for free and get basic access to the community.</p>
      <% end %>
    </div> 
    
  </div>
  <div class="col-md-6 col-md-offset-3">
    <div class="well form-color-style name-font">
    <h2>Sign up</h2>
      <% if params[:plan] == '2' %>
        <%= render "pro_form" %>
      <% else %>
        <%= render "basic_form" %>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>